// Stubbed deprecated micro test: overlay button presence now covered by consolidated
// scenario tests (core smoke + tag UI). This file intentionally exports nothing to
// satisfy historical references while eliminating redundant lint/test surface.
// If reintroducing focused overlay tests, fold assertions into an existing scenario
// per consolidation policy (see copilot-instructions.md section 3.1 / 17).

export {};
